## Technical Questions

1. When would you choose to use a Hash Map (or Dictionary) over a simple array to store data? Describe a scenario where a Hash Map is significantly more efficient and explain the time complexity for insertion and lookup in both data structures.
2. Explain the difference between Depth-First Search (DFS) and Breadth-First Search (BFS) on a graph. For a social network application, which algorithm would be more suitable for finding the shortest path to a "friend of a friend"?
3. You have two tables in a SQL database: `Employees` (with columns `EmployeeID`, `Name`, `DeptID`) and `Departments` (with columns `DeptID`, `DeptName`). Write a SQL query to find the names of all employees who work in the 'Engineering' department.
4. Describe the fundamental principles of Object-Oriented Programming (OOP). How have you applied principles like Encapsulation and Polymorphism in your Python or Java projects?
5. What is a REST API? If you were to design a simple API endpoint for your skin cancer detection model, what would the request and response formats look like for submitting an image and receiving a classification result?