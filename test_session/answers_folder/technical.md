## Technical Questions

**1. How would you implement a thread-safe LRU (Least Recently Used) cache from scratch? Describe the data structures you would choose to ensure efficient get and put operations, and explain the locking mechanism you'd use to handle concurrent access.**
*Model Answer:* I would use two data structures: a hash map for O(1) average time complexity lookups of keys, and a doubly-linked list to maintain the order of usage for O(1) additions and removals. The hash map would store keys and pointers to the nodes in the linked list. When an item is accessed (`get`), it's moved to the head of the list. When a new item is added (`put`), it's also added to the head. If the cache is full, the item at the tail of the list is evicted. To ensure thread safety, I would wrap all `get` and `put` operations with a single mutex or a reentrant lock to prevent race conditions when multiple threads try to modify the cache structures simultaneously.

**2. You are given a stream of numbers and you need to be able to find the median at any point in time. Describe an efficient algorithm and the data structures you would use to solve this. What is the time complexity for adding a number and for finding the median?**
*Model Answer:* The most efficient approach is to use two heaps: a max-heap to store the smaller half of the numbers and a min-heap for the larger half. We maintain a balance where the heaps are either equal in size or the max-heap has one more element. When a new number arrives, we add it to the appropriate heap and then rebalance if necessary by moving the root element from one heap to the other.
*   **Finding the median:** If the heaps have equal size, the median is the average of the two roots. If one is larger, the median is the root of that heap. This is an O(1) operation.
*   **Adding a number:** This involves an insertion into a heap and a potential rebalancing step, both of which take O(log n) time.

**3. Explain the difference between process-based and thread-based concurrency. In the context of Python, how does the Global Interpreter Lock (GIL) affect the performance of CPU-bound vs. I/O-bound multi-threaded applications?**
*Model Answer:* Processes have separate memory spaces and do not share state by default, making them safer but heavier. Threads exist within a single process, share the same memory space, and are more lightweight. In Python, the Global Interpreter Lock (GIL) is a mutex that allows only one thread to execute Python bytecode at a time.
*   **For CPU-bound tasks:** The GIL prevents true parallel execution on multi-core CPUs, so multithreading offers no performance benefit and can even add overhead. For these tasks, multiprocessing is the better choice.
*   **For I/O-bound tasks:** Multithreading is very effective. While one thread is blocked waiting for I/O (like a network request or disk read), the GIL is released, allowing another thread to run.

**4. Consider a very large, sorted array of integers that has been rotated an unknown number of times. How would you efficiently find a specific target integer within this array? Analyze the time complexity of your solution.**
*Model Answer:* I would use a modified binary search. In each step, I'd calculate the middle element. The key is to identify which half of the array (from `left` to `mid` or `mid` to `right`) is still sorted. I can do this by comparing `arr[left]` with `arr[mid]`. Once I know which part is sorted, I can check if the target lies within that sorted range. If it does, I search in that half; otherwise, I search in the other half. This approach eliminates half of the search space in each iteration. The time complexity is O(log n), the same as a standard binary search.

**5. What is database indexing and why is it important for performance? Explain the difference between a B-Tree index and a Hash index, and describe a scenario where one would be significantly more effective than the other.**
*Model Answer:* Database indexing is a technique to speed up data retrieval operations on a database table at the cost of additional writes and storage space. It works by creating a separate data structure that holds a copy of a column's values in a sorted order, with pointers to the corresponding rows.
*   **B-Tree Index:** Stores data in a sorted, tree-like structure. It's highly effective for both point queries (e.g., `WHERE id = 10`) and range queries (e.g., `WHERE age > 30`). This is the default index type in most relational databases.
*   **Hash Index:** Stores a hash of the column value. It's extremely fast for exact equality lookups (`=`) but does not support range queries because the hash values are not ordered.
*   **Scenario:** For finding a user by their unique `email_address`, a Hash index would be more effective due to its O(1) lookup time. For finding all products with a `price` between $50 and $100, a B-Tree index is necessary as it can efficiently handle the range query.